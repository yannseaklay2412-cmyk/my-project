SHARED — code both client and server import.
Validation rules, status enums, type definitions, shared constants.
Stops the classic bug where the frontend allows a value the backend
rejects. Keep it dependency-free: no react, no express.
